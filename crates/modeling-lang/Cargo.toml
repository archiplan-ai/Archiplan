[package]
name = "modeling-lang"
version = "0.2.0"
edition = "2024"
license = "MIT"
description = "Archiplan modeling language"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_path_to_error = "0.1"
toml = "0.8"
