# Trade-off configuration

At the initial steps Archiplan proposes to configure architectural trade-offs: what matters most in the current case, what can be sacrificed: e.g: scalabality can be sacrificed for the simplicity and speed of development.

## Auto-mode

It's not neccessary for users to configure tradeoffs manually, if they choose to, the agent will poll them and find suitable configuration itself.