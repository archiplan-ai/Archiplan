# Self-hosting

Archiplan is its own first user: the tool's architecture is modeled, hardened, versioned and
code-linked in Archiplan itself, so building the tool exercises every stage of the workflow it
prescribes — and every rough edge in that workflow lands on its own maintainers first.
