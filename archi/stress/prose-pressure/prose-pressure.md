---
version: v0008
closed: v0008
---

# Prose pressure

Eleventh round, pressing the definition design before code exists: the intent constrains
definitions to identity prose and names one smuggling pattern — the comma splice carrying a
modal verb. This round hunts the routes around that gate: other splice punctuation, obligation
clauses that need no splice at all, prose arriving through the statement API where no comment
was ever parsed, and the render round-trip that must stay byte-stable once definitions join
the archive.
