---
version: v0004
closed: v0005
---

# Identity pressure

Seventh round, sixth issue replay
(`issues/canonical-render-edge-order-depends-on-module-names.md`): the pressure is on version
identity itself — the replayed phantom version a file rename mints, and three survivors that
fence the fix: a real semantic edit still moves the hash and diffs minimally, canonical renders
still compile and replay (delegation chains included), and every sealed version reconstructs
byte-identically across the contract change — which re-renders the live model once, recorded as
a reorder-only patch.
