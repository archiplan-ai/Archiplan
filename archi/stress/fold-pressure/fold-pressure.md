---
version: v0006
closed: v0007
---

# Fold pressure

Tenth round, pressing the session-fold design before it is modeled: the operator wants
`rounds-fold-deliberately` closed, and the candidate mechanism — the journal's `merge=union`
attribute applied to session files — needs the same empirical treatment round nine gave the
archive. Executed in the two-clone lab: same-slug rounds fused open and sealed, the union
attribute switched on and off, the archive recipe followed faithfully over a fused record, and
a fold synthesized across differing pins.
