---
version: v0005
closed: v0005
---

# Merge pressure

Ninth round, first with two writers: the operator wants archiplan multiplayer — parallel
branches mutating the spec, then merging back to one lineage. Every store in the tree was
pressed through real branch-and-merge cycles (two clones off a shared origin, the release
binary, real conflicts and real resolutions): the version archive under colliding saves, the
model text under the canonical contract, the link journal under racing verbs, and the session
record itself. The execute loop under merge — parallel plans, capture racing capture — is
deliberately out of scope: it gets its own round once the spec-side joins hold.
