# Scenarios

- A plan closes its last wave, `plan next` announces the cleanup once, the next call brings the scenarios, and a legacy plan without the field still completes. Runs on cargo test (plan_e2e, init_e2e — no infrastructure).
