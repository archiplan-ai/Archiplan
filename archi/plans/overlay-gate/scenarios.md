# Scenarios

- An operator maps a member to a leftover agent worktree: the map refuses naming the main checkout; a stale row from an old binary still cannot base a seat — the mint refuses with the repair, and the same mint with an explicit base lands the member beside the main checkout. Runs on cargo test (multi_repo_e2e, worktree_e2e — no infrastructure).
