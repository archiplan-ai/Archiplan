# Scenarios

- An agent asked for dependent work walks the fork through one poll question, forks only through the mint, and names the base aloud; the installed briefing stays byte-equal to the binary. Runs on cargo test (init_e2e — no infrastructure).
