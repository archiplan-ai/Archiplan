# Scenarios

- A user runs `archi check-update` and learns in one line whether a newer version exists, then `archi update` and the binary answers the new number. Runs on cargo test (update_e2e, file:// fixtures — no infrastructure).
