# Scenarios

- A user runs `archi check-update` and learns in one line whether the feed carries a newer release, then `archi update` and the binary answers the new number after a checksum-verified swap. Runs on cargo test (update_e2e, file:// fixtures — no infrastructure).
